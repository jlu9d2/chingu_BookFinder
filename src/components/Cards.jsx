import { useEffect } from 'react';

//takes in the title state from search
//searches the data for the titles
//should return an obj or arr
//use map on data and pass each into card component
const Cards = (bookSearchTitle) => {






  return (
    <div className="cards">
      
    </div>
  )

}

export default Cards;